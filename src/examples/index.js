import React from 'react';
import ReactSalute from '../lib';

const Examples = () => {
    return ( <section >
        <ReactSalute />
        <ReactSalute name="Foo" />
        </section>
    );
};

export default Examples;